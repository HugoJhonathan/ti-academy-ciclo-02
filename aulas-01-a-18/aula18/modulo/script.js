import { calcular, removerProdutos, objeto } from './util.js';
// importa o que foi passado como export

calcular('produtos', 'Somatotal'); // chamada das funções importadas
removerProdutos('produtos');
alert(objeto.nome);